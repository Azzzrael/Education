package glava3_kniga;

public class Logic {
    public static void main(String [] args) {
        double j =1;
        double k =0;
        //j != 0 && 1/j > j+k // не делить на ноль. Вторая часть не вычисляется, если значение переменной х равно нулю.

       // условие ? выражение 1 : выражение_2 // Если условие истинно, то вычисляется первое выражение, а если оно ложно второе выражение.
     //   х < у ? х : у

    }
}
